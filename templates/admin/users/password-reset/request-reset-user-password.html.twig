{% extends 'baseTemplate/registration-base.html.twig' %}

{% set page_title = 'Demande de reinitialisation de mot de passe' %}

{% block content %}
    {% for flashError in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}
    <h1 class="text-5xl font-normal leading-normal mt-0 mb-2 text-indigo-500">Changement de mot de passe</h1>

    {{ form_start(requestForm) }}
        {{ form_row(requestForm.email) }}
        
        <div>
            <small>
                Nous allons envoyer un email à <strong>{{user.lastname}} {{user.firstname}}</strong> pour qu'il puisse avoir un lien pour réinitialiser son mot de passe.
            </small>
        </div>

        <button class="text-indigo-500 background-transparent font-bold uppercase px-3 py-1 text-xs outline-none focus:outline-none mr-1 mb-1 ease-linear transition-all duration-150">Envoyer</button>
    {{ form_end(requestForm) }}
{% endblock %}