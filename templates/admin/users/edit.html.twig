{% extends 'baseTemplate/admin-base.html.twig' %}

{% block title %}Modifier {{ user.username }}{% endblock %}

{% block body %}
    <h1 class="text-5xl font-normal leading-normal mt-0 mb-2 text-indigo-500">Modification de {{ user.username }}</h1>
   {{ include('admin/users/_form.html.twig', {'button_label': 'Update'}) }}
   <a href="{{path('request-reset-user-password', {id: user.id})}}"  class="text-indigo-500 background-transparent font-bold uppercase px-3 py-1 text-xs outline-none focus:outline-none mr-1 mb-1 ease-linear transition-all duration-150">Changer de mot de passe</a>
    <a href="{{ path('users_index') }}">Retour à la liste</a>

    {{ include('admin/users/_delete_form.html.twig') }}
{% endblock %}
