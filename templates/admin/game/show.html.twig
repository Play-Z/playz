{% extends 'baseTemplate/admin-base.html.twig' %}

{% block title %}Jeux{% endblock %}

{% block content %}
    <h1 class="text-5xl font-normal leading-normal my-28 mb-5 text-indigo-500">{{ game.name }}</h1>
    {# TODO: faire design #}

    {# <table class="table">
        <tbody>
        <tr>
            {% if game.path %}
                <th>Logo</th>
                <td><img src="{{ vich_uploader_asset(game, 'logo') }}" width="64" height="auto" alt="Logo de {{ game.name }}"></td>
            {% else %}
                <th>Logo</th>
                <td>Pas de logo</td>
            {% endif %}
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ game.name }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ game.description }}</td>
        </tr>
        <tr>
            <th>Mode de jeux</th>
            {% if game.gametypes %}
                {% for type in game.gametypes %}
                    <td> {{ type.name }} </td>
                {% endfor %}
            {% else %}
                <td>Aucun mode de jeu</td>
            {% endif %}
        </tr>
        </tbody>
    </table>

    <a class="text-indigo-500 background-transparent font-bold uppercase px-3 py-1 text-xs outline-none focus:outline-none mr-1 mb-1 ease-linear transition-all duration-150" href="{{ path('game_index') }}">Retour</a>

    <a class="text-green-500 background-transparent font-bold uppercase px-3 py-1 text-xs outline-none focus:outline-none mr-1 mb-1 ease-linear transition-all duration-150" href="{{ path('admin_game_edit', {'slug': game.slug}) }}">Modifier</a>

    {{ include('admin/game/_delete_form.html.twig') }} #}

    <section class="pt-20 mx-5 my-5 lg:pt-[120px] pb-10 lg:pb-20 bg-white">
        <div class="container">
            <div class="flex flex-wrap">
                    <div class="w-1/3 md:w-1/2 xl:w-1/4 px-4 shadow-2xl mx-10 my-10">
                        <div class="bg-white rounded-lg overflow-hidden mb-10">
                        {% if game.path %}
                            <img
                                src="{{ vich_uploader_asset(game, 'logo') }}"
                                alt="Image du jeu : {{ game.name }}"
                                class="w-1/2 mx-auto"
                                />
                        {% else %}
                            <img
                                src="{{ asset('images/no-image.png') }}"
                                class="w-1/2 mx-auto"
                                />
                        {% endif %}
                            <div class="p-8 sm:p-9 md:p-7 xl:p-9 text-center">
                                <h3>
                                    <a
                                        class="
                                        font-semibold
                                        text-dark text-xl
                                        sm:text-[22px]
                                        md:text-xl
                                        lg:text-[22px]
                                        xl:text-xl
                                        2xl:text-[22px]
                                        mb-4
                                        block
                                        "
                                        >
                                    {{game.name}}
                                    </a>
                                </h3>
                                <p class="text-base text-body-color leading-relaxed mb-7 italic">
                                    {{game.description}}
                                </p>
                                {% if game.gametypes %}
                                <p
                                    {% for type in game.gametypes %}
                                        <p class="text-base text-body-color leading-relaxed mb-7 italic">
                                    {{ type.name }}
                                </p>
                                    {% endfor %}
                                {% else %}
                                    <p>Aucun mode de jeu</p>
                                {% endif %} 
                                <a class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" href="{{ path('admin_game_index') }}">Retour</a>

                            </div>
                        </div>
                    </div>
            </div>
        </div>
    </section>
{% endblock %}