{% extends 'baseTemplate/admin-base.html.twig' %}

{% block title %}Modes de jeux{% endblock %}

{% block content %}
    {# <div class="px-10">
        <h1 class="text-5xl font-normal leading-normal text-center mt-20 mb-2 text-indigo-500">Mode de jeux</h1>
        <div class="grid grid-cols-5 gap-10 ">
            {% for game in games %}

                <div class="max-w-sm rounded overflow-hidden shadow-lg">
                    <img class="w-full" width="150" height="150" src="{{ vich_uploader_asset(game, 'logo') }}" alt="Image du jeu : {{ game.name }}">
                    <div class="px-6 py-4">
                        <div class="font-bold text-xl mb-2">{{ game.name }}</div>
                        <p class="text-gray-700 text-base">
                            {{ game.description }}
                        </p>
                    </div>
                    <div class="px-6 pt-4 pb-2">
                        {% for type in game.gametypes %}
                            <span class="inline-block bg-blue-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2">
                                <a href="{{ path('gametype_edit',{game_slug: game.slug, slug: type.slug}) }}" >{{ type.name }}</a>
                                <a href="{{ path('gametype_delete',{game_slug: game.slug, slug: type.slug}) }}" class=" inline-block px-3 py-1 rounded-full text-red-700 bg-red-200" href="">x</a>
                            </span>
                        {% endfor %}
                    </div>
                    <a class="px-4" href="{{ path('gametype_add',{slug:game.slug}) }}">Ajouter un mode jeu</a>
                </div>
            {% endfor %}
        </div>
    </div> #}
    <!-- ====== Cards Section Start -->
    <h1 class="text-5xl font-normal leading-normal text-center mt-20 text-indigo-500">Mode de jeux</h1>
    <section class="pt-20 mx-5 my-5 lg:pt-[120px] pb-10 lg:pb-20 bg-white">
        <div class="container">
            <div class="flex flex-wrap">
                {% for game in games %}
                    <div class="w-1/3 md:w-1/2 xl:w-1/4 px-4 shadow-2xl mx-10 my-10">
                        <div class="bg-white rounded-lg overflow-hidden mb-10">
                            <img
                                src="{{ vich_uploader_asset(game, 'logo') }}"
                                alt="Image du jeu : {{ game.name }}"
                                class="w-1/2 mx-auto"
                                />
                            <div class="p-8 sm:p-9 md:p-7 xl:p-9 text-center">
                                <h3>
                                    <a
                                        class="
                                        font-semibold
                                        text-dark text-xl
                                        sm:text-[22px]
                                        md:text-xl
                                        lg:text-[22px]
                                        xl:text-xl
                                        2xl:text-[22px]
                                        mb-4
                                        block
                                        "
                                        >
                                    {{game.name}}
                                    </a>
                                </h3>
                                <p class="text-base text-body-color leading-relaxed mb-7 italic">
                                    {{game.description}}
                                </p>
                                <a
                                    href="{{ path('gametype_add',{slug:game.slug}) }}"
                                    class="
                                    inline-block
                                    py-2
                                    px-7
                                    mx-2
                                    my-2
                                    border border-[#E5E7EB]
                                    rounded-full
                                    text-base text-body-color
                                    font-medium
                                    hover:border-blue-500 hover:bg-blue-500
                                    transition
                                    "
                                    >
                                Ajouter un mode de jeux
                                </a>
                                {% for type in game.gametypes %}
                                    <h3>
                                        <a href="{{ path('gametype_edit',{game_slug: game.slug, slug: type.slug}) }}" >{{ type.name }}</a>
                                        <a href="{{ path('gametype_delete',{game_slug: game.slug, slug: type.slug}) }}" class=" inline-block px-3 py-1 rounded-full text-red-700 bg-red-200" href="">x</a>
                                        </a>
                                    </h3>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>
    <!-- ====== Cards Section End -->
{% endblock %}