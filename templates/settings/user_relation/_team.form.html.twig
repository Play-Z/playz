<form method="post" action="{{ path('user_relation_new', {'user_slug' : user.slug }) }}">
    <input type="hidden" name="type" value="team">
    <button class="text-green-500 background-transparent font-bold uppercase px-3 py-1 text-xs outline-none focus:outline-none mr-1 mb-1 ease-linear transition-all duration-150">Inviter dans l'équipe</button>
</form>