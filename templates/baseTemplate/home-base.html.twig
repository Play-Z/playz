{% extends 'baseTemplate/base.html.twig' %}

{% block body %}

{{ include('components/navbar-play.html.twig') }}
    <div class="relative">
        {% block content %}

        {% endblock %}
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="flash-{{ label }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    </div>
{{ include('components/footer.html.twig') }}
{% endblock %}

 