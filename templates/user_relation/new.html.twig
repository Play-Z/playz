<!DOCTYPE html>

<title>New UserRelation</title>

{% block body %}
    <h1>Create new UserRelation</h1>

    {{ include('user_relation/_form.html.twig') }}

    <a href="{{ path('user_relation_index') }}">back to list</a>
{% endblock %}
