<!DOCTYPE html>

<title>Edit UserRelation</title>

{% block body %}
    <h1>Edit UserRelation</h1>

    {{ include('user_relation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('user_relation_index') }}">back to list</a>

    {{ include('user_relation/_delete_form.html.twig') }}
{% endblock %}
