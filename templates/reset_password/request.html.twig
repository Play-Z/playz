{% extends 'baseTemplate/registration-base.html.twig' %}

{% set page_title = 'Mot de passe oublié' %}

{% block content %}
	{# {% for flashError in app.flashes('reset_password_error') %}
						        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
						    {% endfor %}
						    <h1 class="text-5xl font-normal leading-normal mt-0 mb-2 text-indigo-500">Récupération de votre mot de passe</h1>
						
						    {{ form_start(requestForm) }}
						        {{ form_row(requestForm.email) }}
						        <div>
						            <small>
						                Entrez votre adresse email, nous y enverrons un lien pour réinitialiser votre mot de passe.
						            </small>
						        </div>
						
						        <button class="text-indigo-500 background-transparent font-bold uppercase px-3 py-1 text-xs outline-none focus:outline-none mr-1 mb-1 ease-linear transition-all duration-150">Envoyer</button>
						    {{ form_end(requestForm) }} #}

	<section class="h-full gradient-form bg-gray-200 md:h-screen">
		<div class="container py-12 px-6 h-full">
			<div class="flex justify-center px-6 my-12">
				<!-- Row -->
				<div class="w-full xl:w-3/4 lg:w-11/12 flex" style="margin: 12%">
					<!-- Col -->
					<div class="w-full h-auto bg-gray-400 hidden lg:block lg:w-1/2 bg-cover rounded-l-lg" style="background-image: url('https://source.unsplash.com/oWTW-jNGl9I/600x800')"></div>
					<!-- Col -->
					<div class="w-full lg:w-1/2 bg-white p-5 rounded-lg lg:rounded-l-none">
						<div class="px-8 mb-4 text-center">
							<div class="text-center">
								<img class="mx-auto w-48" src="{{ asset('images/logo/PlayZ.png') }}" alt="logo"/>
								<h3 class="pt-4 mb-2 text-2xl">Vous avez oublié votre mot de passe ?</h3>
								<p class="mb-4 text-sm text-gray-700">
									Pas de soucis ! Entrez simplement votre adresse e-mail ci-dessous et nous vous enverrons un lien pour réinitialiser votre mot de passe !
								</p>
							</div>
						</div>
						<form class="px-8 pt-6 pb-8 mb-4 bg-white rounded" method="post">
							{% for flashError in app.flashes('reset_password_error') %}
						        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
						    {% endfor %}
							 {{ form_start(requestForm) }}
								<div class="mb-4">
									<label class="block mb-2 text-sm font-bold text-gray-700" for="email">
										Email
									</label>
									{{ form_widget(requestForm.email) }}
								</div>
								<div class="mb-4">
									<button class="w-full px-4 py-2 font-bold text-white focus:outline-none focus:shadow-outline" style=" background: rgb(83,36,110);
																				background: linear-gradient(90deg, rgba(83,36,110,1) 0%, rgba(107,22,214,1) 35%, rgba(183,152,235,1) 100%);" type="submit">
										Réinitialiser mot de passe
									</button>
								</div>
							 {{ form_end(requestForm) }}
							<hr class="mb-6 border-t"/>
							<div class="text-center">
								<div class="flex items-center justify-between pb-6">
									<p class="mb-0 mr-2">Pas encore inscrit ? Par ici 👉</p>
									<button type="button" class="inline-block px-6 py-2 border-2 border-red-600 text-red-600 font-medium text-xs leading-tight uppercase rounded hover:bg-black hover:bg-opacity-5 focus:outline-none focus:ring-0 transition duration-150 ease-in-out" data-mdb-ripple="true" data-mdb-ripple-color="light">
										<a href="/register">
											Let's go !</a>
									</button>
								</div>
							</div>
							<div class="text-center">
								<div class="flex items-center justify-between pb-6">
									<p class="mb-0 mr-2">Déjà inscrit ? 👉</p>
									<button type="button" class="inline-block px-6 py-2 border-2 border-red-600 text-red-600 font-medium text-xs leading-tight uppercase rounded hover:bg-black hover:bg-opacity-5 focus:outline-none focus:ring-0 transition duration-150 ease-in-out" data-mdb-ripple="true" data-mdb-ripple-color="light">
										<a href="/login">
											Se connecter</a>
									</button>
								</div>
							</div>
						</form>
					</div>
				</div>
			</div>
		</div>
</section>
{% endblock %}
