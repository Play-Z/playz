{% use 'tailwind_2_layout.html.twig' %}

{% block form_row %}
    {% set row_class = row_class|default('w-full md:w-1/2 px-3 mb-6 md:mb-0 ...') %}
    {{ parent() }}
{% endblock form_row %}

{% block form_label %}
    {% set label_class = label_class|default('block text-gray-500 font-bold md:text-right mb-1 md:mb-0 pr-4" ...') %}
    {{ parent() }}
{% endblock form_label %}

{% block form_widget %}
    {% set widget_class = widget_class|default('w-full p-4 text-sm bg-gray-50 focus:outline-none border border-gray-200 rounded text-gray-600 ...') %}
    {{ parent() }}
{% endblock form_widget %}